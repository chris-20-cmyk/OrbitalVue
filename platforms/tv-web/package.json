{
  "name": "@orbitalvue/tv-web",
  "version": "5.8.0-alpha.3",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "check": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --config vitest.config.ts",
    "build": "vite build && node scripts/package-platforms.mjs"
  },
  "dependencies": {
    "@orbitalvue/catalog": "workspace:*",
    "@orbitalvue/media-centers": "workspace:*"
  }
}
